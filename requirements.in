# Direct dependencies for the QUASAR execution image.
#
# Ranges here, exact pins in environment.lock.txt, which is generated by `pip freeze`
# inside the built image and committed. The image tag plus the lock file together define
# the environment that every result record cites.

# numerics
numpy>=2.0,<3
scipy>=1.14

# quantum circuits and simulation (Route A, hardware feasibility)
qiskit>=2.0,<3
qiskit-aer>=0.16

# tensor networks (Baseline C, WP6)
quimb>=1.8
autoray>=0.6
opt_einsum>=3.3

# QSVT phase factors (Route B, WP2)
pyqsp>=0.1.6

# Wright-Fisher acceleration (Baseline A, WP4)
numba>=0.60

# tabulation, storage, figures
pandas>=2.2
pyarrow>=17.0
matplotlib>=3.9
tqdm>=4.66

# testing inside the image, so gates run in the same environment they are judged in
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100
